apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - application-gcp.yaml

  # Optional observability stack. Uncomment when Grafana/Prometheus/Loki/Tempo/OTel are needed.
  # Then run: kubectl apply -k argocd
  # - observability.yaml
